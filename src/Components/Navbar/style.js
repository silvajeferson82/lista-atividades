import styled from 'styled-components';

const Nav = styled.nav`
   display: flex;
   background-color: rgba(0,0,0,0.3);
   padding: 8px;
   color: white;
   font-weight: bold;
`;

export default Nav;